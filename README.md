# Gettng started with JS Project using Node

To work with this project first clone it

```bash
git clone <repo-url>
```

Then run the `npm install` command to install the dependencies

```bash
npm install
```
